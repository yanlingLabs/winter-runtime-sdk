node_modules
dist
*.tgz

# The router repo MUST NOT commit any Anthropic artifact (WS-02 §2). WS-23 dropped the official SDK
# dependency entirely; this comment stays so nobody "fixes" a missing type by copying it in.
